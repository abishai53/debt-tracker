services:
  debt-tracker:
    image: ezraorina834/debt-tracker:latest
    container_name: debt-tracker
    restart: unless-stopped
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - NODE_ENV=${NODE_ENV}
    networks:
      - ezra-home

networks:
  ezra-home:
    external: true
